# Safeway 四人工作分配

本文件以 24 小時 Dev Jam、2 位前端與 2 位後端為前提。分工採用「API 契約先行、檔案所有權明確、各自可用 mock／fixture 獨立開發」的方式，目標是讓四個人從第一小時就能同時工作，並降低整合時互相覆蓋程式碼的風險。

## 一、共同目標與範圍

### P0：比賽結束前必須完成

- 使用者可輸入或選擇目的地。
- App 能顯示目前位置、最快步行路線及至少一條替代路線。
- 後端能對候選路線計算 deterministic、可解釋的安全分數。
- 每條路線顯示距離、時間、安全分數、資料信心、理由與警告。
- 資料不足時必須降低信心並顯示警告，不得把未知資料當作安全。
- Gemini 只能解釋後端已計算的結果，不得自行產生分數或地理事實。
- 至少有一條固定 Demo 路線能從前端走完整個後端流程。
- API key 不得提交到 Git、前端程式或 APK。

### P1：P0 穩定後再完成

- 顯示沿線營業中的求助據點 Marker。
- 加入一個示範區的犯罪或照明開放資料。
- 支援定位拒絕、目的地找不到、上游配額及 API 失敗畫面。
- Cloud Run 部署與真機測試。
- 保留 Gemini 工具呼叫與後端錯誤紀錄，但不得記錄精確起訖位置。

### P2：時間充足才處理

- Flutter Web 備用展示頁。
- 更多偏好選項、動畫及視覺效果。
- 透過不同 waypoint 增加候選路線。
- 正式登入、使用紀錄或資料庫。

## 二、角色總覽

| 角色 | 主責 | 主要交付成果 | 建議分支 |
|---|---|---|---|
| 前端 A：地圖與裝置整合 | Flutter 骨架、定位、Google Map、Polyline、Marker | 可獨立接收 mock 路線資料並畫在地圖上的 `SafewayMap` | `feat/frontend-map` |
| 前端 B：使用者流程與 API 串接 | 搜尋、API client、資料 model、結果卡片、狀態處理 | 從輸入目的地到顯示路線比較卡片的完整流程 | `feat/frontend-flow` |
| 後端 A：地理資料與 API 平台 | FastAPI、Routes、Places、GeoJSON、例外處理、部署 | 能回傳候選路線及可驗證原始 metrics 的 `/v1/navigate` | `feat/backend-routing-data` |
| 後端 B：安全評分與 Gemini | 評分、confidence、warning、Gemini 說明、單元測試 | 不依賴外部 API、可用 fixture 測試的安全評估模組 | `feat/backend-scoring-agent` |

角色名稱是檔案所有權及責任範圍，不代表其他人不能協助。跨區修改前必須先通知該檔案負責人，由負責人整合或明確同意共同修改。

## 三、開始開發前的共同契約

開始後的前 30–60 分鐘，四人一起確認 README 中的 `POST /v1/navigate` request／response，並以 README 的 JSON 作為第一版凍結契約。

共同決定以下項目後才各自切分支：

1. Demo 行政區與兩組固定起訖點。
2. `NavigationRequest`、`NavigationResponse`、`RouteResult` 欄位名稱與型別。
3. `confidence` 固定使用 `high`、`medium`、`low`。
4. 後端錯誤格式固定包含 `code`、`message`、`retryable`。
5. 路線顏色、選取狀態及「最快／較安全」標籤規則。
6. 真實 API 不可用時要啟用的 Demo fixture，畫面必須標示為示範資料。

契約凍結後，由後端 A 擔任 `backend/app/schemas.py` 的檔案負責人，由前端 B 擔任 `frontend/lib/models/` 的負責人。其他人若需要新增欄位，先在團隊頻道提出用途，兩端同意後再修改，避免前後端各自發明格式。

## 四、每個人的工作細則

### 前端 A：地圖與裝置整合

#### 負責檔案

- `frontend/android/`
- `frontend/ios/`
- `frontend/web/` 中與 Google Maps 啟動相關的設定
- `frontend/lib/services/location_service.dart`
- `frontend/lib/widgets/safeway_map.dart`
- 地圖樣式及 Polyline、Marker 相關 widget
- `frontend/pubspec.yaml` 的套件整合

`flutter create frontend` 只由前端 A 執行一次，完成後立即 commit，避免兩個人各自產生不同的 Flutter 骨架。

#### P0 任務

1. 建立 Flutter 專案並加入 `google_maps_flutter`、`geolocator` 及 HTTP client 所需套件。
2. 完成 Android／iOS 位置權限設定；定位被拒絕時回傳明確狀態，不讓 App 卡死。
3. 建立 `SafewayMap`，透過參數接收：
   - 目前位置
   - 起點與終點
   - 多條 route polyline
   - `selectedRouteId`
   - 求助據點 Marker
   - `onRouteTap(routeId)` callback
4. 定義路線視覺規則：推薦路線、最快路線、未選取替代路線必須能明確辨識。
5. 點擊 Marker 時顯示名稱、類型、營業狀態與資料來源；未知營業狀態不得顯示為營業中。
6. 使用固定 mock route 驗證多條 Polyline、Marker、縮放範圍及路線選取，不等待後端完成。

#### P1 任務

- 夜間地圖樣式。
- 目前位置追蹤及重新置中按鈕。
- 真機定位、旋轉螢幕及 App 回到前景後的狀態測試。
- Android Maps SDK key 的 package／SHA-1 限制；不得使用後端 server key。

#### 對其他人的輸出介面

前端 B 只需傳入 `List<NavigationRoute>`、`selectedRouteId` 與 Marker 資料，不必知道 Google Maps SDK 的內部實作。前端 A 不直接呼叫後端 API，也不自行解析 `/v1/navigate` JSON。

#### 驗收條件

- 不連後端也能用 mock 資料顯示至少兩條不同路線。
- 切換選取路線時，Polyline 樣式與地圖資訊同步更新。
- 拒絕定位權限後 App 仍可使用手動起點或顯示可理解提示。
- 原始碼及 APK 中不存在 Gemini 或 Maps server key。

### 前端 B：使用者流程與 API 串接

#### 負責檔案

- `frontend/lib/main.dart`
- `frontend/lib/screens/map_screen.dart`
- `frontend/lib/models/navigation_request.dart`
- `frontend/lib/models/navigation_result.dart`
- `frontend/lib/services/safeway_api.dart`
- `frontend/lib/widgets/destination_search.dart`
- `frontend/lib/widgets/route_summary_card.dart`
- Loading、empty、warning、error state 相關 widget

#### P0 任務

1. 依凍結契約建立 request／response model，所有 nullable 欄位都要能安全解析。
2. 建立 `SafewayApi`：
   - API URL 由 `--dart-define=API_BASE_URL=...` 注入。
   - 設定合理 timeout。
   - 將 `400`、`404`、`429`、`502` 轉為使用者可理解的錯誤狀態。
3. 完成目的地輸入、送出、取消及重新搜尋流程。
4. 建立路線比較卡片，固定顯示：
   - 「最快」或「推薦的較安全路線」標籤
   - 距離與預估時間
   - 安全分數及 confidence
   - 可驗證 reasons
   - 資料缺漏 warnings
5. 使用 README 的固定 JSON 完成整個畫面，不等待真實 API。
6. 將解析後的 route／Marker 資料交給前端 A 的 `SafewayMap`，並管理目前選取路線。
7. 畫面持續顯示「安全分數是輔助資訊，不能保證安全；緊急狀況請立即求助」。

#### P1 任務

- 重試按鈕、離線提示及上游配額提示。
- 起訖點、路線卡片及地圖之間的狀態一致性。
- 搜尋輸入 debounce，避免重複 API request。
- Demo 模式明確標示「示範資料」，不得與即時資料混淆。

#### 對其他人的輸出介面

前端 B 負責將後端 JSON 轉為前端 model。若解析失敗，要回報缺少的欄位及 response 範例給後端 A；不得直接在畫面層以硬編碼補造安全資料。

#### 驗收條件

- 使用 mock response 可完整走過搜尋、Loading、成功、警告及錯誤流程。
- 真實後端切換只需要更改 `API_BASE_URL`，不需修改程式碼。
- 缺少 `warnings` 或部分 metrics 時不會 crash。
- 使用者可在最快與較安全路線之間手動切換。

### 後端 A：FastAPI、Routes、Places 與資料查詢

#### 負責檔案

- `backend/app/main.py`
- `backend/app/schemas.py`
- `backend/app/config.py`
- `backend/app/services/routes.py`
- `backend/app/services/places.py`
- `backend/app/services/datasets.py`
- `backend/app/data/`
- `backend/tests/test_routes.py`
- `backend/tests/test_places.py`
- `backend/requirements.txt`
- `backend/Dockerfile`
- `data_sources.md`

#### P0 任務

1. 建立 FastAPI 專案、`GET /healthz` 與 `POST /v1/navigate`。
2. 依 README 建立 Pydantic schemas，設定座標範圍、偏好與時間格式驗證。
3. 先完成 mock `/v1/navigate`，讓前端在第一個整合點就能連線。
4. 實作目的地解析及 Routes API client：
   - `WALK`
   - `computeAlternativeRoutes: true`
   - 最小必要 field mask
   - timeout、quota、無結果及 upstream error
5. 對 encoded polyline 產生固定距離採樣點，輸出後端 B 所需的 `RouteCandidate`。
6. 實作 Places 查詢與去重，僅查明確可求助的地點；營業資料不存在時標為 `unknown`。
7. 載入示範 GeoJSON，檢查格式、資料日期、授權及覆蓋區域；資料不足時回傳 coverage warning。
8. 將後端 B 提供的 `evaluate_route(...)` 與 `compose_route_explanation(...)` 接入 `/v1/navigate`。
9. 統一錯誤格式：
   - `400`：座標或偏好不合法
   - `404`：找不到目的地
   - `429`：上游配額不足
   - `502`：Routes、Places 或 Gemini 上游失敗

#### P1 任務

- CORS allowlist、request 大小限制及簡單 rate limit。
- Docker、Cloud Run 與 Secret Manager 部署。
- Cloud Logging 遮罩精確 origin／destination、Authorization header 及 API key。
- 針對 Routes／Places 建立可重播的 fixture，避免測試時持續消耗 API 配額。

#### 對其他人的輸出介面

後端 A 產生的每條 `RouteCandidate` 至少包含：

```text
id
encoded_polyline
distance_meters
duration_seconds
sampling_points
nearby_help_points
police_proximity_data
crime_features
lighting_features
data_sources
```

外部來源沒有資料時使用空集合及明確 coverage metadata，不得填入看似合理的假數字。

#### 驗收條件

- `/healthz` 穩定回傳 `{"ok": true}`。
- API key 缺失時啟動錯誤或 endpoint 錯誤清楚，不洩漏 key。
- Routes／Places timeout、配額及無結果都有一致錯誤格式。
- 每個資料欄位可以追溯來源、日期與覆蓋範圍。
- 不記錄完整精確起訖位置。

### 後端 B：安全評分、Confidence 與 Gemini

#### 負責檔案

- `backend/app/services/safety_score.py`
- `backend/app/services/gemini_agent.py`
- `backend/app/services/confidence.py`
- `backend/tests/fixtures/route_candidates.json`
- `backend/tests/test_safety_score.py`
- `backend/tests/test_confidence.py`
- `backend/tests/test_gemini_agent.py`
- Gemini system instruction、structured output schema 及 agent trace 規則

後端 B 不等待 Routes／Places 完成。第一小時先依 README 與共同契約建立固定 `RouteCandidate` fixture，後續外部 API 只要輸出相同格式即可接入。

#### P0 任務

1. 實作純函式 `evaluate_route(route, preferences)`：
   - 原始指標先正規化為 `0..1`。
   - 缺少資料時移除該權重，再將剩餘權重重新正規化。
   - 回傳 score、metrics、confidence、reasons、warnings。
   - 相同輸入必須得到相同結果，不呼叫 LLM。
2. 實作 detour 規則：超過使用者允許繞路時間時不推薦，或給予明確大幅扣分。
3. 實作 confidence：
   - 依資料覆蓋率、更新時間、來源種類與缺漏欄位判斷。
   - 缺少路燈資料不能視為零風險，必須降低 confidence 並產生 warning。
   - 過期資料不得和即時資料使用相同信心。
4. 產生 deterministic reasons，例如「沿途有 5 個營業中求助據點」；只有 metrics 確實支持時才能輸出。
5. 實作 Gemini 說明層：
   - 只接收已完成的 route evaluation。
   - 使用 structured output 或 function calling。
   - 不允許修改 score、metrics、confidence 或 warnings。
   - Gemini 失敗時回退到 deterministic reasons，整個導航 API 仍須可用。
6. 設定 agent 上限：timeout、最多工具呼叫次數及失敗後 fallback，避免無限循環與費用失控。
7. 建立單元測試：
   - crime risk 增加時分數下降。
   - 求助據點增加時分數不下降。
   - 繞路超標時不得推薦。
   - 缺資料時 confidence 下降並出現 warning。
   - 全部安全資料缺失時不得輸出高信心。
   - Gemini 回傳格式錯誤或 timeout 時使用 deterministic fallback。
   - LLM 不得改寫數值。
8. 提供後端 A 可直接呼叫的穩定介面：

```python
evaluate_route(route, preferences) -> RouteEvaluation
compose_route_explanation(evaluation) -> RouteExplanation
```

#### P1 任務

- 比較 fastest route 與 recommended route，產生「多走幾分鐘換取哪些指標改善」的說明。
- 記錄 Gemini model、延遲、token usage、成功／fallback 狀態；不得記錄精確座標。
- 建立三組固定評測案例：正常資料、部分缺漏、互相矛盾或過期資料。
- 與團隊共同確認評分公式不會因單一指標造成極端結果。

#### 對其他人的輸出介面

`RouteEvaluation` 至少包含：

```text
score: 0..100
confidence: high | medium | low
metrics: 原始可驗證指標
reasons: deterministic reasons
warnings: 缺漏、過期或 coverage 警告
eligible_for_recommendation: boolean
```

Gemini 只輸出 `title` 與改寫後的繁中 `reasons`；所有數字及 warning 以 deterministic evaluation 為唯一真實來源。

#### 驗收條件

- 不設定任何 API key，也能跑完全部評分與 confidence 測試。
- 固定 fixture 的分數可重現。
- 所有分數都有 metrics 可解釋，沒有由 Gemini 產生的事實。
- Gemini 無法使用時，API 仍能回傳 deterministic 說明。
- 評分、警告與 fallback 測試全部通過後才允許整合進主流程。

## 五、避免互相阻塞的介面切點

| 生產者 | 消費者 | 約定交付物 | 對方可先使用的替代品 |
|---|---|---|---|
| 後端 A | 後端 B | `RouteCandidate` | `route_candidates.json` fixture |
| 後端 B | 後端 A | `RouteEvaluation` 與兩個純函式 | 固定 score／reason mock |
| 後端 API | 前端 B | `/v1/navigate` JSON | README response fixture |
| 前端 B | 前端 A | `NavigationRoute`、Marker、選取狀態 | 前端本地 mock model |
| 前端 A | 前端 B | `SafewayMap` widget | 暫時 placeholder container |

任何人被另一人的功能阻塞超過 20 分鐘時，先使用表中的替代品繼續工作，再把介面差異記錄到團隊頻道；不要停下來等待對方完成。

## 六、Git 協作規則

1. 四人各自在指定 feature branch 工作，不直接在 `main` 寫功能。
2. 開始一個工作區段前執行 `git pull --rebase origin main`；有衝突就停止並和檔案負責人處理，不得 force-push。
3. Commit 保持單一目的，例如：
   - `feat(frontend): render alternative route polylines`
   - `feat(api): integrate walking routes client`
   - `feat(scoring): handle missing lighting coverage`
   - `test(scoring): add detour threshold cases`
4. Pull request 說明必須包含：完成項目、測試方式、已知限制、是否需要其他人同步介面。
5. `main` 必須保持可啟動；尚未完成的功能使用 feature flag、mock 或 fallback，不提交會讓全隊無法執行的半成品。
6. 共用檔案只設一位負責人：
   - `frontend/pubspec.yaml`：前端 A
   - `frontend/lib/main.dart`：前端 B
   - `backend/app/schemas.py`：後端 A
   - `backend/requirements.txt`：後端 A
7. 需要新套件或 schema 欄位時，把名稱、用途與版本交給負責人統一修改。
8. API key、`.env`、service account JSON、Maps key 不得 commit；只提交 `.env.example` 的變數名稱。

## 七、24 小時整合節點

| 時間 | 四人應達成狀態 | 整合驗證 |
|---|---|---|
| 0–1h | 契約、Demo 區域、fixture、分支確定 | README JSON 能被前後端各自解析 |
| 1–4h | 前端有 mock 地圖與結果卡；後端有 healthz、mock navigate、評分測試 | App 可連 mock API；評分不需外部服務即可執行 |
| 4–8h | Routes／Places client 初版；安全評分與 confidence 完成 | 一條候選路線可從 RouteCandidate 變成 RouteEvaluation |
| 8–12h | 前後端第一次真實串接；GeoJSON 或 coverage warning 確定 | 真實 API 或明確 Demo fallback 能走完整流程 |
| 12–16h | Gemini 說明、錯誤狀態、Marker、資料來源顯示 | 關閉 Gemini 或任一資料源後，系統仍能合理降級 |
| 16–20h | Cloud Run／真機測試、隱私與金鑰檢查 | 固定 Demo 起訖點連續成功三次 |
| 20–22h | 停止新增功能，只修 P0 問題 | 依驗收清單跑完整流程並錄製備援影片 |
| 22–24h | 簡報、Demo 演練及備援資料 | 每位成員都能在斷網或 API 配額不足時切換 Demo fixture |

## 八、整體 Definition of Done

一個功能只有符合以下條件才算完成：

- 已提交到自己的 feature branch，且 commit 訊息能說明目的。
- 有成功案例，也處理最可能的錯誤或缺資料案例。
- 不含 API key、個資或精確歷史犯罪點位。
- 沒有把 mock／fixture 包裝成即時真實資料。
- 介面符合共同契約；若契約有變更，前後端 model 同步更新。
- 負責人已在本機實際執行，不只確認程式可以編譯。
- 合併到 `main` 後仍能啟動，且不破壞其他人的 P0 流程。

## 九、功能刪減順序

時間不足時，依下列順序刪減，不能犧牲核心可信度：

1. 先刪動畫、美化、更多偏好及 Flutter Web。
2. 再刪多個行政區、更多資料源及 waypoint 候選路線。
3. Gemini 無法穩定時改用 deterministic reasons，不影響評分。
4. 路燈或犯罪資料不可得時，移除其權重、降低 confidence 並顯示 warning。
5. 保留最小核心：真實步行候選路線、可求助據點、deterministic 評分、資料限制說明及可操作的前端。

不得為了 Demo 看起來完整而捏造犯罪、照明、營業狀態或替代路線資料。
